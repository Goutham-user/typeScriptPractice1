var drawPoint = function (point) {
    console.log("This is drawpoint function" + x + "value of y " + y);
};
// encapsualting the data of point ehich is the parameter of above function
var point;
({
    x: 5,
    y: 2
});
drawPoint(point);
